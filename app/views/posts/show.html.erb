<div class="container">
  <%= cl_image_tag @post.photo.key, height: 300, width: 400, crop: :fill %>
  <h1><%= @post.title %></h1>
    <%= @post.content %>
      <%= link_to "Edit", edit_post_path(@post)  %>
</div>
